[package]
name = "winfolsize"
version = "0.1.0"
edition = "2024"
description = "A Windows disk space visualizer with treemap and sunburst views"

[dependencies]
eframe = "0.31"
rfd = "0.15"
humansize = "2"

[profile.release]
opt-level = 3
lto = true
strip = true
